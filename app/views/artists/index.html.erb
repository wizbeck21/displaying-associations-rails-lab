<h1>Artists</h1>
<p><%=pluralize(@artists.count, 'Artist') %></p>
<ul>
  <% @artists.each do |a| %>
    <li><%= link_to a.name, artist_path(a) %> - <%= pluralize(a.song_count, 'song') %> </li>
  <% end %>
</ul>
